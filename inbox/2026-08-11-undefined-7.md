---
date: 2026-08-11
description: "Closed out the session: added a CHANGELOG.md [1.8.0] entry covering the DOB/address tags, the 98-tag production sweep, and the new docx-surgeon crate,"
tags:
  - project-note
source_repo: bmaksimov
---

# 

Closed out the session: added a CHANGELOG.md [1.8.0] entry covering the DOB/address tags, the 98-tag production sweep, and the new docx-surgeon crate, bumped Cargo.toml/Cargo.lock to 1.8.0 (commit 92a3779), and created git tag v1.8.0 -- matching the project's own established tag-per-version convention (v1.0.0 through v1.5.0 are all tagged). Judged no separate "bug fix" patch bump was warranted: the only candidate (the Dropbox-metadata-sidecar cleanup commit) is repo hygiene, not a product defect, and the dedup-session-404 incident from earlier needed no code change at all. Pushed everything: unitprep-api main + the v1.8.0 tag, unitprep-ui main (just the qms-tags page split, 8efd3ee -- judged no version bump needed there since it's a pure no-behavior-change refactor). Explicitly left three pre-existing, unrelated uncommitted items in unitprep-ui untouched per Boris's direct confirmation (app/layout.tsx, public/favicon.svg, public/orchestrator-logo-dark.svg -- separate in-progress work, not part of this session's effort).

Noticed and worth flagging next time it comes up, not acted on unprompted: v1.6.0 and v1.7.0 in unitprep-api were never git-tagged, breaking the otherwise-consistent v1.0.0-v1.5.0 tag-per-version pattern. Also unitprep-ui's own current version (1.4.0) has no tag either, and its last real tag is v1.3.1. Neither gap was backfilled -- Boris didn't ask for that, only for tonight's new work to be tagged appropriately.







_Recorded 2026-08-11T17:58:35.736Z from `bmaksimov` via the om MCP server (routing: fallback)._
