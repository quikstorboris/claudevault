---
date: 2026-08-11
description: "Split app/(app)/admin/client-ops/qms-tags/page.tsx (458 lines, flagged by Boris in a modularity code-review pass) into four files: styles.ts (shared b"
tags:
  - project-note
source_repo: bmaksimov
---

# 

Split app/(app)/admin/client-ops/qms-tags/page.tsx (458 lines, flagged by Boris in a modularity code-review pass) into four files: styles.ts (shared button/input class constants, previously would have been duplicated across the split pieces), CreateTagForm.tsx (the new-tag form, self-contained including its own create request), TagFilters.tsx (the three filter controls, purely presentational -- parent still owns the actual filter values since it needs them to compute visible rows), and TagRow.tsx (one table row, owning its own local edit-input buffer -- resets from the tag's current values each time that row enters edit mode -- while the parent still owns which row is being edited, since only one can be at a time and the parent renders the whole list). page.tsx dropped to 229 lines, keeping only the tag list, editingKey/pendingKey, and filter state -- everything genuinely needed at the parent level because rendering the table requires it. No behavior change, same UI. Verified via tsc/eslint (clean) and a live dev server (route compiles, resolves correctly, no console errors -- same signed-out-redirect verification depth as before, still can't complete WebAuthn from this sandboxed browser to check the authenticated table itself). Committed as 8efd3ee.







_Recorded 2026-08-11T17:19:41.349Z from `bmaksimov` via the om MCP server (routing: fallback)._
