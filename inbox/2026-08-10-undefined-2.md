---
date: 2026-08-10
description: "Shipped the Phase 1 admin UI for the client_ops.qms_tag catalog (the gap flagged while resolving the client_ops vault inconsistency: backend shipped 2"
tags:
  - project-note
source_repo: bmaksimov
---

# 

Shipped the Phase 1 admin UI for the client_ops.qms_tag catalog (the gap flagged while resolving the client_ops vault inconsistency: backend shipped 2026-08-07 with zero frontend). Added lib/clientOps.ts (its own small fetch client separate from lib/auth.ts, since the deactivate/reactivate endpoints are PATCH, a method lib/auth.ts's authFetch doesn't support) and app/(app)/admin/client-ops/qms-tags/page.tsx (table of all tags with add/inline-edit/deactivate/reactivate, gated by RequirePermission on client_ops.manage_tags, following the Users/Roles page conventions). Wired a "QMS Tags" entry into LeftNav.tsx's ADMINISTRATION_LINKS and extended LeftNav.test.tsx's coverage to include it (all 10 tests pass). tsc --noEmit and eslint both clean on the new/changed files. Verified in a real browser against live unitprep-api (:8080) + unitprep-ui (:3000) dev servers: unauthenticated access to the new route correctly redirects to /login (confirms RequirePermission/layout gating engages), no unexpected console/network errors. Could NOT verify the authenticated CRUD flow itself (add/edit/deactivate/reactivate against the real table) — that needs a real WebAuthn passkey ceremony (Windows Hello/Proton Pass), which the sandboxed browser tool can't perform; left both dev servers running for Boris to check that part himself.







_Recorded 2026-08-10T16:48:32.871Z from `bmaksimov` via the om MCP server (routing: fallback)._
